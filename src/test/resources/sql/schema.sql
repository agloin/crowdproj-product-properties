CREATE TABLE IF NOT EXISTS product_properties_test
(
    id  VARCHAR(256) not null,
    name VARCHAR(256),
    description VARCHAR(256) NOT NULL,
    units VARCHAR(256) NOT NULL,
    unitMain VARCHAR(256) NOT NULL
);